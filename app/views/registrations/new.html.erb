<%= simple_form_for(@registration) do |f| %>
  <% if @registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@registration.errors.count, "error") %> prohibited this registration from being saved:</h2>

      <ul>
      <% @registration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<p><strong>Title:</strong><%= @course.title %></p>
<p><strong>Description:</strong><%= @course.description %></p>
<p><strong>Teacher:</strong><%= @course.teacher %></p>

<!-- < form_tag(courses_path, controller: "courses", action: "register") do %>
	< email_field(:student, :email) %>
	< submit_tag %>
< end %> -->
	Email <input name="student_email" type="email" autofocus="true"><br>
	<!-- <input name="title" type="hidden" value= -->
	<%= hidden_field_tag("title", @course.title) %>
	<%= hidden_field_tag("date_complete", @course.end_date) %>
	<%= f.hidden_field :course_id, value: params[:id] %><br>
<!-- 	< f.hidden_field :title, value: @course.title %><br>
	< f.hidden_field :date_complete, value: @course.end_date %><br>
 -->
<!-- < f.hidden_field :student_id, value: '1' %>  -->
Send Your Class Record to SkillChest? <input name="send_to_skillchest" type="checkbox"><br><br>
<!-- < f.check_box :send_to_skillchest %> -->
<%= f.submit %><br>
<%= link_to 'Back', courses_path %>
<% end %>